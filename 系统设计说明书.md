这份系统设计说明书（System Design Document, SDD）旨在将前述需求分析（SR）转化为具体的工程架构。它不仅关注功能的实现，更聚焦于**软硬件接口的协同（STCO）**、**时序一致性**以及**异常收敛策略**。

---

# LPDDR6 弹性视图切换系统设计说明书 (SDD)

## 1. 系统模块架构 (System Component Architecture)

系统被划分为四个垂直层级，通过标准的内核接口和私有的 SMC 指令集进行交互。

### 1.1 硬件层 (Hardware Layer - LPDDR6 & DMC)

* **DMC (DDR Memory Controller)**: 负责物理地址到通道的映射。支持两套解码逻辑：全通道交织（4-Ch）与簇交织（2-2 Ch）。
* **Power Gate Control**: 负责 PHY 2 和 PHY 3 的独立电源域切断。
* **NOC Isolation**: 在 PHY 下电期间，拦截发往失效通道视图的请求，反馈 AXI Error 响应。

### 1.2 固件层 (Firmware Layer - ATF/EL3)

* **State Machine Service**: 管理物理层状态机（Initialization -> Active -> Self-Refresh -> Power-Down）。
* **Register Shadowing**: 在切换期间保护 DDR 配置寄存器，防止非法指令写入。

### 1.3 内核驱动层 (Kernel Space - V-Topology Manager)

* **Block State Manager**: 以 128MB 为颗粒度管理 128 个物理 Section 的逻辑视图状态。
* **Hotplug Wrapper**: 封装内核 `offline_pages` 与 `online_pages` 接口。
* **NEON Migration Engine**: 调用高效的向量化拷贝指令集，执行物理数据的重排。

### 1.4 监控层 (User Space/Kernel Monitor)

* **DDR Traffic Monitor**: 监测各通道带宽利用率。
* **Predictive Policy Engine**: 结合 Input Event (中断) 与 Cgroup 负载进行切换决策。

---

## 2. 详细设计：数据流与视图映射 (Data Flow & View Mapping)

### 2.1 物理 Section 状态转移

每个 128MB 的 Section 必须处于以下四个状态之一：

1. **ACTIVE_A (4-Ch)**: 映射在 Node 0，带宽最大。
2. **MIGRATING**: 正在执行 memcpy，该区域对应用不可见。
3. **ACTIVE_B (2-2 Ch)**: 映射在 Node 1，功耗最优。
4. **FIXED_B**: 1GB 基座区域，禁止移动。

### 2.2 逻辑地址别名 (Alias) 设计

采用**硬编码偏移**方案：

* **View A 基地址**: `0x0_8000_0000` (PA_START)
* **View B 基地址**: `0x4_8000_0000` (PA_START + 16GB)

---

## 3. 关键流程设计 (Key Process Design)

### 3.1 降级切换流程 (4-Ch -> 2-2 Ch)

这是核心的省电路径，重点在于**排水与重排**。

1. **Selection**: Monitor 选定待迁移的 128MB Section。
2. **Isolation**: 驱动通知 MM 子系统，将该 Section 标记为不可分配（Reserved）。
3. **Draining**: 调用 `offline_pages`。内核自动将页面迁移至其他 Node 0 区块。
4. **Transformation (NEON)**:
* CPU 读取 View A 地址空间。
* CPU 写入 View B 地址空间（触发物理层数据重新排列）。


5. **Validation**: 刷新 Cache，确保数据在物理存储单元（DRAM Cells）中持久化。
6. **De-activation**: 检查若 Node 0 已无活跃 Block，固件执行 PHY 2,3 下电。

### 3.2 快速唤醒流程 (2-2 Ch -> 4-Ch)

这是性能路径，重点在于**预判与并发**。

1. **Trigger**: 监测到特定输入（Touch/Binder/GPU IRQ）。
2. **PHY Wakeup**: 发起异步 SMC，PHY 2,3 在后台上电校准（1ms）。
3. **Online**: 将 Section 从 Node 1 迁移回 Node 0。
4. **QoS Control**: 限制切换瞬间的访存频率，防止电轨电压不稳（Voltage Sag）。

---

## 4. 软件重排引擎优化 (Software Reordering Engine)

为了将 128MB 搬运控制在 8ms 以内，设计如下优化：

* **指令优化**: 使用 `LDP/STP` (Load/Store Pair) 配合 128-bit Q 寄存器。
* **非临时访存**: 使用 `STNP` 指令旁路 L3 Cache，减少对前台应用的缓存污染。
* **多线程并行**: 在高负载切换场景下，利用两个大核并行执行 128MB 的重排，理论带宽可提升 80% 以上。

---

## 5. 异常处理设计 (Exception Handling)

### 5.1 热插拔失败 (Hotplug Timeout)

* **原因**: 页面被内核 Long-term Pin（如 DMA 锁定）。
* **对策**: 驱动程序将该 Block 标记为“暂时不可切”，Monitor 寻找下一个候选 Block，避免系统挂死。

### 5.2 视图访问冲突 (Aliasing Conflict)

* **现象**: CPU 在切换中途尝试访问旧视图。
* **对策**: 利用 ARM64 的 **Permission Indirection (PIE)** 机制，在迁移期间将旧视图页表设置为无访问权限，由 `do_page_fault` 进行拦截与等待。

---

## 6. 系统接口规格 (Interface Specification)

### 6.1 Sysfs 接口

* `/sys/bus/vmem/current_mode`: 读取/设置当前全局视图模式。
* `/sys/bus/vmem/block_map`: 显示 128 个 Block 的详细视图分布。

### 6.2 固件 SMC 接口

* `SMC_SET_PHY_POWER(mask)`: 控制物理层电源开关。
* `SMC_NOTIFY_VIEW_CHANGE(range, mode)`: 通知硬件地址解码器逻辑变更。

---

## 7. 结论

本 SDD 通过**分层防御**和**分批迁移**的思路，解决了由于硬件缺失 DMA 带来的重排开销问题。通过 128MB 的步进式管理，系统可以在不重启、不掉帧的前提下，动态调整 DDR 的物理层运行模式。

**下一步行动建议：**
这份设计说明书已完成。如果您确认，我们可以进入**代码原型（Prototype）开发阶段**，我会为您编写 **V-Topology Manager 驱动的头文件骨架**，包含上述状态机和 SMC 调用接口的 C 语言定义。