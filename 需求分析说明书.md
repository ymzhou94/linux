这份需求分析说明书（Software Requirements Specification, SRS）站在系统架构师视角，针对 **LPDDR6 弹性视图切换与分通道下电系统** 进行了高度抽象与规格化。它将复杂的软硬协同逻辑转化为可量产、可验证的工程需求。

---

# LPDDR6 弹性存储视图与动态通道管理需求分析说明书

## 1. 引言 (Introduction)

### 1.1 目的

定义一种基于 ARM64 内核与 LPDDR6 硬件特性的存储优化系统。通过 128MB 粒度的热插拔和物理视图 Alias 映射，实现 4-Ch 与 2-2 Ch 模式的动态切换，以在 Android 系统中达到极致的能效比。

### 1.2 背景

现代移动终端面临高性能（4-Ch 带宽）与长待机（2-Ch 功耗）的矛盾。由于硬件自动重排引擎暂未成熟，本方案通过软件定义拓扑（STCO）解决该痛点。

---

## 2. 系统概述 (System Overview)

系统由物理层（LPDDR6 PHY）、固件层（ATF/EL3）、内核驱动层（V-Topology Manager）及监控层（Monitor）组成。

### 2.1 核心组件

* **物理层**: 支持单通道独立下电（Power-Gating）。
* **DMC (DDR Memory Controller)**: 提供 View A (4-Ch) 与 View B (2-2 Ch) 的基地址别名映射。
* **V-Topology Manager**: 内核驱动，执行 128MB 块的热插拔与 NEON 优化搬运。
* **DDR Monitor**: 实时感知访存压力与系统事件。

---

## 3. 功能需求 (Functional Requirements)

### 3.1 视图管理需求

* **FR-1: Alias 空间注册**：系统必须能识别同一 16GB 物理内存对应的 0-16GB (View A) 和 16-32GB (View B) 地址空间。
* **FR-2: 虚拟 NUMA 绑定**：内核须将 View A 和 View B 映射为独立的虚拟 NUMA 节点。
* **FR-3: 128MB 粒度控制**：内存管理必须支持以 128MB 为步进进行 `online/offline` 操作。

### 3.2 切换与搬运需求

* **FR-4: 软件重排 (Software Reordering)**：在无硬件 DMA 时，系统必须通过 NEON 向量指令集执行 View 间的数据物理重排。
* **FR-5: 原子性保证**：切换期间必须锁定对应 Section，防止 CPU 或外设（DMA）访问正在搬运的地址。
* **FR-6: 状态同步**：切换完成后，必须强制执行全系统缓存清洗（Cache Cleaning）。

### 3.3 硬件协同需求

* **FR-7: PHY 下电控制**：当 View A 无活跃块时，驱动须触发 SMC 指令关闭 PHY 2, 3。
* **FR-8: 快速唤醒**：PHY 从 Power-Down 到 Ready 的恢复时间必须控制在 **1ms** 以内。

---

## 4. 非功能需求 (Non-Functional Requirements)

### 4.1 性能指标 (Performance)

* **128MB 切换时延**：单块 memcpy 搬运及热插拔总耗时应 **< 10ms**。
* **UI 稳定性**：视图切换过程中，系统 UI 丢帧率（Drop Frame Rate）增量应 **< 1%**。
* **预判准确率**：Monitor 唤醒 PHY 的预判成功率应 **> 85%**。

### 4.2 功耗指标 (Power Consumption)

* **静态功耗收益**：在 2-2 Ch 模式下，DDR 系统静态功耗应较 4-Ch 降低 **30% 以上**。

### 4.3 兼容性与安全性 (Safety & Compatibility)

* **内核兼容性**：必须支持 Android 主流内核版本（5.15+ / 6.1+），且支持 `ZONE_MOVABLE`。
* **访问保护**：在 PHY 下电期间，非法视图访问必须通过 NOC 拦截并上报错误，不得导致内核挂死。

---

## 5. 逻辑架构与数据流 (Logical Architecture)

### 5.1 内存分布模型 (Memory Distribution Model)

* **Anchor Zone (32-48GB)**: 存放内核镜像，不参与任何视图切换，确保心跳稳定。
* **Elastic Zone (0-32GB)**: 动态区域，根据负载在 Node 0 与 Node 1 之间流转。

---

## 6. 关键技术风险与对策 (Risk Assessment)

| 风险点 | 影响程度 | 缓解对策 |
| --- | --- | --- |
| **Section 离线失败** | 高 | 强制标记 `ZONE_MOVABLE`；切换前触发 `drop_caches`。 |
| **Cache Aliasing** | 极高 | 严格执行 `clflush` 指令；利用物理地址不连续性隔离视图。 |
| **1ms 唤醒延迟突发请求** | 中 | 实现 IOMMU 端的 QoS 限流，平滑流量冲击。 |
| **电源域切换噪声** | 低 | 固件层增加电轨稳定等待时间（Wait-state）。 |

---

## 7. 验收标准 (Acceptance Criteria)

1. **功能验证**：通过 `/proc/buddyinfo` 观察内存块在不同 NUMA 节点间的成功迁移。
2. **功耗验证**：使用电表对比 View A 模式与 View B 模式（PHY 2,3 Off）的底电流差异。
3. **稳定性验证**：进行 7x24 小时连续视图切换压力测试，无 Kernel Panic。

---

**后续建议：**
这份 SRS 是项目开发的“宪法”。基于此，我们可以开始编写 **DTS (Device Tree) 配置脚本** 或 **内核驱动的头文件定义 (vmem_mgr.h)**。您是否需要我为您列出该驱动核心结构体的具体 C 语言定义？