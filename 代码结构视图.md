为了确保方案从架构设计平滑过渡到工程实现，我们需要定义一个清晰的**驱动程序代码结构视图**。该结构遵循 Linux 内核驱动开发规范（基于 `platform_driver` 框架），并集成了我们讨论的虚拟 NUMA、128MB 热插拔及 NEON 搬运逻辑。

以下是 **V-Topology Manager** 的代码组织架构设计：

---

## 1. 驱动源代码目录结构 (Directory Layout)

```text
drivers/memory/
├── Makefile                # CONFIG_LPDDR6_VTOPOLOGY 构建入口
├── Kconfig                 # 内核配置项 (CONFIG_LPDDR6_VTOPOLOGY)
├── lpddr6_vtopology.c      # 原型驱动：sysfs 视图/块状态管理
└── vmem_mgr/               # 完整驱动规划目录（后续扩展）
    ├── Makefile
    ├── Kconfig             # 内核配置项 (CONFIG_VMEM_VIEW_MANAGER)
    ├── vmem_mgr_core.c     # 驱动入口、状态机控制逻辑
    ├── vmem_mgr_numa.c     # 虚拟 NUMA 节点注册与管理
    ├── vmem_mgr_hotplug.c  # 封装内核 offline/online 接口
    ├── vmem_mgr_reorder.c  # NEON 向量化 memcpy 实现 (重排引擎)
    ├── vmem_mgr_smc.h      # 定义与 ATF/EL3 通信的 SMC 调用号
    └── vmem_mgr.h          # 核心结构体与全局宏定义

```

---

## 2. 核心数据结构设计 (vmem_mgr.h)

驱动的核心在于对 128MB 区块（Section）的状态追踪。

```c
/* 视图模式定义 */
enum ddr_view_mode {
    DDR_VIEW_4CH = 0,    /* View A: 4-Channel Interleave */
    DDR_VIEW_2CH = 1,    /* View B: 2-2 Channel Interleave */
    DDR_VIEW_STATIC = 2, /* View C: No-Interleave (Fixed) */
};

/* 128MB 内存区块描述符 */
struct vmem_block {
    int id;                     /* 0 - 127 */
    unsigned long phys_addr;    /* 对应的真实物理基地址 */
    enum ddr_view_mode current_view;
    struct mutex lock;          /* 保护该区块的迁移原子性 */
    struct device *mem_dev;     /* 关联到内核 memory subsystem 的设备指针 */
};

/* 全局控制器结构体 */
struct vmem_manager {
    struct vmem_block blocks[128];
    unsigned long view_a_base;  /* 0x0_8000_0000 */
    unsigned long view_b_base;  /* 0x4_8000_0000 */
    struct workqueue_struct *wq; /* 用于执行异步 memcpy 的工作队列 */
    int active_phy_mask;        /* 当前上电的 PHY 掩码 (e.g., 0xF or 0x3) */
};

```

---

## 3. 功能模块逻辑拆解

### 3.1 虚拟 NUMA 初始化 (`vmem_mgr_numa.c`)

该模块在驱动 `probe` 阶段调用，主要任务是拦截设备树中的内存定义并注册节点。

* **`vmem_init_numa_nodes()`**: 调用 `numa_add_memblk()`。将 View A 映射到 Node 0，View B 映射到 Node 1。
* **`vmem_set_node_movable()`**: 将 Node 0/1 的内存段通过 `memblock_mark_hotplug()` 标记，确保后续能被 `offline_pages` 成功回收。

### 3.2 热插拔封装层 (`vmem_mgr_hotplug.c`)

负责与 Linux 内核内存管理子系统交互。

* **`vmem_offline_section(block_id)`**: 调用 `offline_pages()`。此过程会触发内核的页面迁移（Page Migration），将数据挪到其他在线的 128MB 块。
* **`vmem_online_section(block_id, view_mode)`**: 根据目标视图计算逻辑地址，调用 `online_pages()`，并设置 `ZONE_MOVABLE`。

### 3.3 软件重排引擎 (`vmem_mgr_reorder.c`)

这是性能的核心，负责在热插拔间隙执行数据物理重整。

* **`neon_reorder_copy(dst, src, size)`**:
* 使用 `kernel_neon_begin()` 开启 SIMD 权限。
* 实现针对 128MB 优化的循环拆解（Loop Unrolling）拷贝逻辑。
* ****（展示向量化读取 4-Ch 数据并顺序写入 2-Ch 地址的流水线映射）。



---

## 4. 驱动运行状态流视图

1. **Probe 阶段**:
* 从 DTS 读取 16GB 物理地址和 Alias 基地址。
* 初始化 128 个 `vmem_block` 结构。
* 注册 Sysfs 接口。


2. **Runtime 阶段 (以降级切换为例)**:
* **Monitor** 触发事件 -> `vmem_mgr_schedule_work()`。
* **Offline 线程**: 选定 Block -> 锁定 Block -> `offline_pages`。
* **Reorder 线程**: 执行 NEON `memcpy`（从 View A 到 View B 地址）-> `clflush`。
* **Firmware 交互**: SMC 调用修改解码逻辑 -> (可选) 下电 PHY。
* **Online 线程**: `online_pages` 到 Node 1。



---

## 5. 软件先行方案的异常闭环

为了保证代码的鲁棒性，结构视图中还包含了一个 **“回滚机制” (Rollback Handler)**：
如果 `offline_pages` 因为内存压力过大在 500ms 内无法完成，驱动将放弃本次迁移，释放 Block 锁，并向系统发出 `MEMORY_PRESSURE_WARNING`，建议 LMK (Low Memory Killer) 介入回收后台进程内存。

**下一步：**
这份代码视图定义了项目的工程底座。如果您认为该结构合理，我可以为您编写 **`vmem_mgr_reorder.c` 中的 NEON 核心拷贝函数代码**，它是整个软件方案中技术含量最高、最影响性能的部分。您想看看它的底层汇编实现吗？
